export enum SchoolBaseServiceStrategy {
  BY_ID,
  BY_SCHOOL,
  BY_SCHOOL_MANAGER
}

export enum AcademicYearServiceStrategy {
  BY_ID,
  BY_SCHOOL,
  BY_SCHOOL_MANAGER
}

export enum StudentServiceStrategy {
  BY_ID,
  BY_SCHOOL,
  BY_SCHOOL_MANAGER
}

export enum SubjectServiceStrategy {
  BY_ID,
  BY_DEPARTMENT,
  BY_SCHOOL
}

export enum TeacherServiceStrategy {
  BY_ID,
  BY_SCHOOL,
  BY_SCHOOL_MANAGER
}
